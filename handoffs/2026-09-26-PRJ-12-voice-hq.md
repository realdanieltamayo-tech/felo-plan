# Handoff — Voice, part 2: natural voice in Felo HQ (built 2026-09-26, branch voice-hq e77327e)

- voice/ (felo-v2 repo, now in the .gitignore allowlist): felo-voice sidecar = python:3.12-alpine + edge-tts,
  aiohttp POST /speak {text, voice?} -> audio/mpeg, GET /health. Voices allowed: Ava/Andrew/Emma Multilingual (default Ava).
  Runs on box 100: `docker run --name felo-voice --network felo-codex-preview --network-alias felo-voice`, read-only,
  cap-drop ALL, 128 MB, restart unless-stopped. Rebuild/restart: /root/felo-v2-work/voice/run.sh (not part of the app
  deploy). Test: 200, ~1.4 s for a short sentence.
- app/lib/voice.js: POST /api/hq/speak -> sidecar (FELO_VOICE_URL, default http://felo-voice:8080/speak), 15 s timeout,
  text cleaned for speech (no code/links/markdown, <= 1,200 chars ending at a sentence). Gate: POST /api/hq/speak.
- app/hq.html: speak() plays the natural voice; falls back to browserSpeak() (old speechSynthesis code); mic start stops
  playback; Settings text updated.
- Watchdog: new check "Felo HQ voice" (felo-voice container running; 3 failures before alert).
- Telegram (part 1) also got session_reset: mode both (4 am Central daily or 6 h idle), notify off — the Telegram chat had
  run since 2026-09-07 and Felo mixed old v1 "read BRIEF.md" messages (sent from Daniel's own account on Sep 7) with new ones.
- Tests: tests/voice.test.cjs; 349 pass.
