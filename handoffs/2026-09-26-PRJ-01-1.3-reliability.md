# Handoff — PRJ-01 1.3 Reliability (DONE 2026-09-26)

## 1. Setup files in git (nightly)
- /usr/local/sbin/felo-ops-snapshot (Proxmox host), cron /etc/cron.d/felo-ops-snapshot 03:20 -> felo-plan
  ops/snapshot/{host,box100,box101} (+ README, SKIPPED.md), commit + push (GitHub mirror 03:40).
  Collects felo-* commands/cron/services, box 100 runtime + deploy/backup/mirror scripts + crontab, box 101 Hermes
  config.yaml (keys blanked: Bearer/api_key -> <REDACTED>), SOUL.md, Felo-made skills, felo-team.py, share/preview
  services, old builder code (no *.sqlite, state, recovery). Never *.env; every file secret-scanned (dropped if it matches).
  First run: 114 files, commit a2e6663. `DRY=1 felo-ops-snapshot` lists what would be saved.

## 2. Scheduled jobs were broken (fixed)
- Cron on the host has PATH=/usr/bin:/bin; `pct` is in /usr/sbin. The off-site backup FAILED on 2026-09-25 for database,
  box100, box101 (host part ok), and the share-button watcher failed every minute since it was installed (manual tests
  passed). Fixed: PATH line in every /etc/cron.d/felo-* + PATH set inside felo-offsite-backup, felo-ops-snapshot,
  felo-share-watch. Re-ran the backup with the cron PATH: all ok (2026-09-26 01:22Z). Share watcher runs clean.

## 3. Down-alerts
- /usr/local/sbin/felo-watchdog, cron /etc/cron.d/felo-watchdog every 5 min, log /var/log/felo-watchdog.log,
  state /var/lib/felo-watchdog/state.json. Checks: box 100/101 running, Felo app, database, lead form, Hermes (checked
  from box 100: its firewall drops everything else, even loopback), share server, felostudio.com, off-site backup
  (ok and < 30 h), disk (host /, local-lvm, both boxes), work PC Ollama (60 min, quiet 22-07 CT), Claude Code login.
  Two failures in a row -> one ntfy alert (plain words, opens Servers page); "working again" on recovery. Channel name read
  from box 100 (from its disk via pct mount if box 100 is down); never stored or printed. `felo-watchdog --test`.
- Bug found and fixed: an em dash in the alert title made ntfy sends fail silently (headers are latin-1).
- First real alert: felostudio.com HTTP 530 — core offline since ~01:20Z 2026-09-26 (Wi-Fi). Daniel to check core.

## 4. Key rotation
- The Felo tools key (FELO_MCP_KEY, Hermes -> Felo tools) was printed once during the secret check. Rotated: new key in
  CT100 tools.env (applied, app healthy) and CT101 config.yaml (Hermes restarted); Felo tools tested OK. Backups:
  tools.env.before-rotate-*, config.yaml.before-rotate-*.

## Open (1.4 cleanup, waiting for Daniel's OK)
- CT100: 7 stopped felo-codex-preview-pre-* containers (old rollback points), docker build cache (~140-360 MB).
- Workbench (old builder) test projects: project test, Memory Plan Check, Chat Project Check, Felo Workflow Check,
  Frontend Agent Check, Server Data Check, Backend Workflow Verification, Saved records workflow verification
  (+ old "Dogo Group LLC" builder version). 6 of them show on the Waiting page.
- v1 (felo-orchestrator containers, image, DB) kept until 2026-10-24 as decided.
- Windows Codex bridge backup: skipped (Codex not used now).
