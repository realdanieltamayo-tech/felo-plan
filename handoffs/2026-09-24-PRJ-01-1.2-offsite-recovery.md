# Handoff · PRJ-01 · 1.2 Off-site copy & recovery · 2026-09-24

**Status: done.** All items verified live, including a full test restore.
**Next phase: PRJ-01 · 1.3 Reliability** — do not start without Daniel's go.
Earlier handoffs: PRJ-00 (map of the system), PRJ-01 1.1 (lock down).

## What exists now
### GitHub copy
- Private repos `realdanieltamayo-tech/felo-v2` (code, all branches + tag `archive-codex-snapshot-2026-09-24`) and `realdanieltamayo-tech/felo-plan` (plan + handoffs).
- Each has its own **write deploy key** on CT100: `~/.ssh/felo_v2_github`, `~/.ssh/felo_plan_github`; SSH aliases `github-felo-v2` / `github-felo-plan` in `~/.ssh/config.felo`. GitHub host key checked against the published fingerprint.
- `/root/felo-github-mirror.sh` (CT100) runs from `post-receive` hooks in both bare repos (seconds after every push/deploy) and nightly 03:40 (cron). Failure → phone alert. Log `/var/log/felo-github-mirror.log`.
- `/root/felo-plan/.git/hooks/post-commit` pushes plan commits to the bare repo automatically (→ GitHub).
- Before the first upload the whole history of both repos was scanned for current secrets: 0.

### Off-site encrypted backups (restic)
- Store: Hostinger VPS **168.231.66.195** (Boston; also runs retired tyx/Postiz/Temporal and core's restic store `/opt/felo-restic`).
- Felo writes as **`felo-backup`**: SFTP only, chrooted to `/srv/felo-backup`, key file `/etc/ssh/authorized_keys.d/felo-backup`, repository `/srv/felo-backup/restic`. It cannot see core's backups or get a shell.
- Proxmox host: key `/root/.ssh/felo_offsite_backup`, alias `felo-offsite` in `/root/.ssh/config`, settings `/root/.felo-backup/env`, password `/root/.felo-backup/restic-password` (600). **Daniel saved the password in his password manager** ("Felo backup key", helper `felo-backup-key`). Without it the backups cannot be opened.
- Job `/usr/local/sbin/felo-offsite-backup`, cron `/etc/cron.d/felo-offsite-backup` nightly **04:30** (after the 04:00 vzdump). Parts, each with its own tag: `database` (pg_dump of felo_preview), `box100` (felo-v2-runtime, .ssh, git repos, felo-plan, ops scripts, app + intake code, crontab), `box101` (.hermes, bin, worker + supervisor code, systemd units), `host` (/etc/pve, helpers, fail2ban, cron.d). Sundays or `--full`: both latest vzdump files + keep policy (14 daily / 8 weekly / 6 monthly, grouped by tags) + `restic check --read-data-subset=5%`. Failure → phone alert. Log `/var/log/felo-offsite-backup.log`, status `/root/.felo-backup/last-status`.
- First run: 6.5 GB stored (mostly the container copies); VPS has ~138 GB free.

### Hostinger VPS hardening (tyx is retired, so the VPS is no longer off-limits)
- `/etc/ssh/sshd_config.d/05-felo-hardening.conf`: `PermitRootLogin prohibit-password` + the `felo-backup` Match block. Backup of the previous settings: `/root/felo-changes-20260924T194155Z/` on the VPS.
- fail2ban (sshd, 5 tries/10 min → 1 h) with the office IP **98.39.229.94** exempt (core and Felo connect from there).
- Felo's root key removed; only `core-backup` remains for root (confirmed at removal). Felo has **no admin access** to the VPS now; any future VPS change needs Daniel to re-add a key in the Hostinger panel.

### Test restore
`/usr/local/sbin/felo-restore-test` (Proxmox host) restores the latest `database` + `box100` from Boston, loads a throwaway database, compares every table with live, starts a sealed trial Felo from the restored code + settings, then shreds everything.
Result 2026-09-24: 39/39 tables, 616/616 rows identical to live; trial Felo "preview is ready"; owner check 403 as expected.

**How to restore for real (summary):** `. /root/.felo-backup/env; restic snapshots` → `restic restore latest --tag database --target /tmp/r` → load into Postgres; `restic restore latest --tag box100 --target /tmp/r` → untar settings/repos; a whole box: `restic restore latest --tag containers --target /tmp/r` → `pct restore <id> <vzdump file>`. If the Proxmox host itself is gone: install restic anywhere, use the password from Daniel's password manager, and a key authorised for `felo-backup` (re-add via root on the VPS).

## Mistakes caught this phase
- The keep policy first grouped three parts under one tag and deleted two of them; fixed with one tag per part and re-backed up the same day.

## Open / carried forward
- IDEAS: shut down retired tyx/Postiz/Temporal on the VPS (smaller attack surface next to the backups).
- 1.3: chat backup brain (needs Daniel's privacy decision), down-alerts for the Servers page, box 101 code into git (it is only in backups so far), optional self-hosted alert server.
- 1.4: remove 7 old `felo-codex-preview-pre-*` containers; v1 leftovers after 2026-10-24.
