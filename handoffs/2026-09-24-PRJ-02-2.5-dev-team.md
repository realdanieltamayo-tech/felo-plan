# Handoff — Step 2.5: Felo hands out the coding work (DONE 2026-09-24)

## Decision (Daniel)
Claude is the main brain AND the only coder for now (backend + frontend). Codex is NOT used yet (saved in IDEAS.md).
Coder is paid by Daniel's Claude subscription (not API credits) — cheaper; the catch is plan usage limits.

## What exists (all on CT101 felo-hermes)
- Claude Code CLI 2.1.197 installed in Hermes' persistent docker sandbox home:
  `/root/.hermes/sandboxes/docker/default/home/.npm-global/bin/claude` (inside sandbox: `/root/.npm-global/bin/claude`).
- Login: Daniel ran `felo-claude-login` (on the Proxmox host, /usr/local/bin) → credentials saved only in the sandbox home
  (`.claude/.credentials.json`). Re-run it if the login ever expires. Claude never handled the credential.
- Dev-team command: sandbox `/root/.felo-team/felo-team.py` (source copy: host scratchpad; put in git in 1.3):
  `build <project> "<task>"` · `review <project>` · `jobs` · `setup-check`.
  Projects: sandbox `/workspace/projects/<name>` (git repo each, one commit per job). Logs: `/workspace/jobs/<job>/`.
- Restricted coder: `--permission-mode acceptEdits --allowedTools Read Edit Write Glob Grep Bash(npm test:*) Bash(npm run test:*) Bash(python3 -m pytest:*)`,
  model opus, max 150 turns, 60-min limit, ANTHROPIC_API_KEY removed from its env (subscription only). No deploy/push/email/network commands.
  (The unrestricted "bypassPermissions" design was blocked by Claude's safety guard; restricted is the chosen design.)
- Hermes skill: `/root/.hermes/skills/autonomous-ai-agents/felo-dev-team/SKILL.md` (how to write tasks, run in background with notify,
  review, report; going live = Level 3).
- SOUL.md / identity/FELO.md team line: all coding → Claude Code via felo-dev-team.

## Proof
- Direct job: team-test quote.js, 8 tests pass.
- Through Felo (API conversation felo-test-2-5): Felo ran setup-check, delegated roundCents job (bc0a68f), ran review (14 tests pass),
  spot-checked, approved, and flagged a real -0 edge case on its own.
- Note: Hermes caps foreground terminal runs at 600 s; long jobs go background automatically.

## Open / next
- Sample project `team-test` left in the sandbox (harmless; delete anytime).
- Installing npm packages happens only in `review` (Felo's terminal), not by the coder. If big projects need the coder to install
  packages, revisit the allowed tools with Daniel.
- 1.3: put felo-team.py + skill + SOUL.md into git (CT101 code into git).
- Next steps per ROADMAP: remaining 2.x handoffs, then PRJ-01 1.3 reliability.
