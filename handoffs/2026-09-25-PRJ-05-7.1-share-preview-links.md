# Handoff — Step 7: client share-preview links (built 2026-09-25; public switch = Daniel)

## What exists (CT101 felo-hermes)
- Container `felo-share` (nginx:1.27-alpine; read-only, caps dropped, 128 MB, restart unless-stopped) on 127.0.0.1:8096.
  Config /etc/felo-share/default.conf: serves ONLY /s/<id of 20+ url-safe chars>/ from the shares folder (read-only mount);
  everything else 404, dotfiles 404, no directory listing, GET/HEAD only, noindex + no-store headers. Tested.
- Shares folder: /root/.hermes/sandboxes/docker/default/workspace/shares/s (Felo's sandbox sees /workspace/shares/s).
- Expiry: /usr/local/sbin/felo-share-expire, cron /etc/cron.d/felo-share-expire (hourly :07) deletes shares past expires_epoch
  (or without a valid .share.json). Tested.
- felo-team.py: `share <project> <days 1-30> --approved-by-daniel` (refuses without the flag) copies a frozen snapshot of
  pages/styles/scripts/images/fonts only (no .md notes, code, package files, dotfiles, tests), prints link + end date + whether
  public is on; `shares`; `unshare <project|id>`. Tested: PLAN.md/.env/package.json not copied.
- felo-dev-team skill v1.4.0: sharing = LEVEL 3 (ask Daniel every time), check page first, Felo gives link to Daniel (does not
  email the client).
- Proxmox host: `felo-share-public on|off|status` = Tailscale Funnel :443 -> 127.0.0.1:8096 on box 101.
  Public URL form: https://felo-hermes.tail0ff06a.ts.net/s/<id>/  (tailnet allows funnel on 443/8443/10000).
  Claude's safety guard refused to turn on a public tunnel itself; Daniel turns it on. Status at handoff: OFF.

## Dogo Group preview (PRJ-03, Daniel drives it via Felo chat)
- Job 20260925-174531-build (Daniel's) done: bilingual one-page pitch, 6 free-license photos (CREDITS.md), form sends nothing,
  [Phone]/[Email] placeholders, check-site PASS. My duplicate relay job 20260925-174451 was stopped (it overlapped briefly).

## Open
- Branded preview address (e.g. preview.felostudio.com) instead of the ts.net name — IDEAS.
- Put felo-team.py, skills, share config into git (PRJ-01 1.3).
